# Expected Outcome

After ralph-codex-e2e executes this plan:
- src/greet.js implements greet(name) returning "Hello, {name}!"
- npm test passes (2/2 tests green)
- Git commit exists: "Feat: Implement greet function"
